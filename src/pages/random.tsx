"use client";

import React, { useEffect } from "react";

const Random: React.FC = () => {
  useEffect(() => {
    // Add JavaScript logic to animate the circles if needed
  }, []);

  return (
    <div className="h-[100vh] bg-blue-600" id="Random">Hello There</div>
  );
};

export default Random;
